# ⛔ Claude — 이 폴더에 들어왔으면 이것부터 읽어라

이 폴더는 **메타(페이스북·인스타그램) 광고를 대신 만들어 주는 도구**다.
사용자가 소재 파일(영상·이미지)을 주면, 네가 캠페인부터 광고 라이브까지 만든다.

**사용자는 개발자가 아니다.** 도구·토큰·API 같은 말을 처음 들을 수 있다.
사용자가 "폴더 확인해" 정도만 말했어도, 아래 판단을 **네가 알아서** 하고 먼저 말을 걸어라.

---

## 지금 무엇을 할지 — 이 순서로 스스로 판단하라

### 1. 세팅이 됐는지 먼저 확인한다 (사용자에게 묻지 말고 직접)

```bash
ls config.json .token 2>/dev/null
```

| 결과 | 상태 | 할 일 |
|---|---|---|
| 둘 다 없음 | **처음 받은 상태** | → **`SETUP.md`를 읽고 세팅을 진행한다** (아래 2번) |
| `.token`만 있음 | 토큰만 있고 계정 설정 전 | → `SETUP.md`의 4단계(계정·페이지 찾기)부터 진행 |
| 둘 다 있음 | 세팅 완료 | → `python3 scripts/setup_wizard.py verify` 로 확인 후 3번 |

### 2. 세팅이 안 됐으면 — 사용자에게 이렇게 먼저 말을 건다

> 메타 광고를 대신 만들어 드리는 도구입니다.
> 소재(영상·이미지)를 주시면 캠페인부터 만들어서 광고 켜는 것까지 해드립니다.
>
> 처음이시니 세팅부터 하겠습니다. 5~10분 걸리고, 브라우저에서 몇 번 클릭하실 일이 있습니다.
> 제가 하나씩 알려드릴게요. 시작할까요?

승낙하면 **`SETUP.md`를 읽고 그 문서에 적힌 대로** 0단계부터 진행한다.
`SETUP.md`에는 "사용자가 개발자 앱도 토큰도 없는 상태"를 전제로 한 안내가 단계별로 있다.

### 3. 세팅이 돼 있으면 — 광고를 만들 준비가 된 것이다

> 세팅은 되어 있습니다. 소재 폴더를 주시면 광고를 만들어 드릴게요.
> 광고는 **꺼진 상태로** 만들어지고, 확인하신 뒤 "켜줘"라고 하셔야 돈이 나갑니다.

사용자가 소재를 주면 **`SKILL.md`를 읽고** 그 절차대로 진행한다.

---

## 문서 지도 — 필요할 때만 읽어라 (한꺼번에 다 읽지 말 것)

| 파일 | 언제 읽나 |
|---|---|
| **`SETUP.md`** | 세팅이 안 돼 있을 때. 앱 생성·토큰 발급·계정 설정 전 과정 |
| **`SKILL.md`** | 실제로 광고를 만들 때. 명령·순서·함정이 전부 여기 있다 |
| `reference/api_guide.md` | Graph API·MCP 상세가 필요할 때 (지표 뜻, 필드명, 파싱) |
| `reference/creative_features.md` | 크리에이티브 개선사항(문구 최적화 등)을 물어봤을 때 |
| `reference/tools.md` | MCP 도구 목록이 필요할 때 |
| `README.md` | **사람이 읽는 설치 안내.** 네가 참고할 내용은 거의 없다 |

---

## ⛔ 무슨 일이 있어도 지킬 것

1. **광고를 켜는 것(ACTIVE)과 삭제는 돈·복구 불가와 직결된다.** `SKILL.md`의 3중 확인 절차를 반드시 거친다.
   끄기·예산 낮추기는 확인 없이 즉시 해도 된다(손해를 막는 방향).
2. **광고 계정 선택은 반드시 사용자 확인을 받는다.** 엉뚱한 계정에 광고비가 나가면 되돌릴 수 없다.
3. **토큰을 화면에 출력하지 마라.** 파일에 저장하고 "저장했습니다"까지만 말한다.
4. **세팅 중에는 광고를 만들지 마라.** `setup_wizard.py`·`check_token.py` 외 실행 금지.
5. 전문용어를 쓰기 전에 한 줄로 풀어서 설명한다. 사용자가 모른다고 하면 되묻지 말고 다시 풀어준다.

---

## 자주 나올 상황

| 사용자가 이렇게 말하면 | 이렇게 한다 |
|---|---|
| "폴더 확인해" / "이거 뭐야" | 위 1번 판단 → 세팅 안 됐으면 2번, 됐으면 3번 |
| "세팅해줘" | `SETUP.md` 읽고 진행 |
| "이 폴더 소재로 광고 만들어줘" | `SKILL.md` 읽고 진행 |
| "광고 켜줘" | `SKILL.md`의 3중 확인 절차 |
| "성과 어때?" | `SKILL.md` 성과 조회 부분 |
| 에러가 났다 | `SETUP.md` 맨 아래 "안 될 때" 표 참고 |
