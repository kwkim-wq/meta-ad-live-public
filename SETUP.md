# 세팅 안내 (Claude가 읽고 진행하는 문서)

> **사용자에게**: 이 폴더를 Claude Code에 열고 이렇게 말하면 됩니다.
> **"SETUP.md 보고 세팅해줘"**
> 나머지는 Claude가 처음부터 끝까지 안내합니다. 준비물 없이 시작해도 됩니다.

---

## Claude에게 — 반드시 이 문서대로 진행하라

**사용자는 개발자가 아니다.** "API 토큰", "앱 ID", "권한(scope)" 같은 말을 처음 들을 수 있다.
**토큰이 아예 없는 상태가 기본이라고 가정하고 시작하라.**

지켜야 할 태도:
- 전문용어를 쓰기 전에 **한 줄로 풀어서** 말한다 (예: "토큰 = 광고 계정을 열 수 있는 열쇠 문자열")
- 브라우저에서 할 일은 **어느 페이지 → 어느 버튼**까지 구체적으로 알려준다
- 한 번에 한 단계씩. 여러 단계를 몰아서 시키지 않는다
- 사용자가 "이게 뭔지 모르겠다"고 하면 **되묻지 말고 다시 풀어서 설명**한다
- 막히면 "안 됩니다"로 끝내지 말고 **무엇이 없어서 막혔는지** 짚어준다

---

## 0단계 — 지금 뭐가 있는지 먼저 확인한다

세팅에 필요한 건 4가지다. 사용자에게 **한꺼번에 묻지 말고** 아래 순서로 하나씩 확인하라.

| 필요한 것 | 확인 질문 | 없으면 |
|---|---|---|
| ① 페이스북 페이지 | "광고에 쓰실 페이스북 페이지가 있나요?" | 페이지부터 만들어야 한다 (아래 A) |
| ② 광고 계정 | "메타(페이스북/인스타) 광고를 집행해 보신 적 있나요?" | 비즈니스 관리자에서 만들어야 한다 (아래 B) |
| ③ 개발자 앱 | 묻지 말고 **없다고 가정** | 1단계에서 같이 만든다 |
| ④ 액세스 토큰 | 묻지 말고 **없다고 가정** | 2단계에서 같이 만든다 |

①②는 이미 광고를 돌리고 있는 회사면 대부분 있다. ③④는 개발용이라 **거의 없다.**

**A. 페이지가 없다면** → https://www.facebook.com/pages/create 에서 먼저 만들게 안내한다.
**B. 광고 계정이 없다면** → https://business.facebook.com 에서 비즈니스 계정을 만들고,
비즈니스 설정 → 계정 → 광고 계정 → 추가로 만든 뒤 **결제 수단을 등록**해야 한다.
이 두 가지는 시간이 걸리니, 없으면 여기서 멈추고 준비되면 다시 부르라고 안내한다.

---

## 1단계 — 파이썬 확인

```bash
python3 --version    # macOS
python --version     # Windows
```

없으면 https://www.python.org/downloads/ 에서 설치하도록 안내한다.
⚠ **Windows는 설치 첫 화면에서 `Add python.exe to PATH` 체크가 필수**다. 빠뜨리면 명령이 안 먹는다.
설치 후에는 **터미널을 새로 열어야** 인식된다.

---

## 2단계 — 개발자 앱 만들기 (사용자가 브라우저에서)

사용자에게 이렇게 설명하고 시작하라:

> 메타에서 광고를 프로그램으로 다루려면 "개발자 앱"이라는 걸 하나 등록해야 합니다.
> 앱을 배포하거나 심사받는 게 아니라, **내 광고 계정에 접근할 열쇠를 만들기 위한 등록 절차**입니다.
> 5분이면 됩니다. 제가 단계별로 알려드릴게요.

1. https://developers.facebook.com/apps 접속 → 페이스북 계정으로 로그인
   - 처음이면 "개발자 등록" 화면이 뜬다. 이름·이메일 확인만 하면 된다
2. **앱 만들기** 클릭
3. 앱 이름은 아무거나 (예: `광고자동화`)
4. 용도를 고르라고 하면 **비즈니스** 선택
5. 만들어지면 왼쪽 메뉴 **이용 사례**(Use cases) → **이용 사례 추가**
6. 목록에서 **"광고 MCP 서버로 광고 만들기 및 관리하기"**
   (영문: `Create & manage ads with ads MCP server`) 추가

> **6번 항목이 목록에 없으면** — 메타가 아직 그 계정에 이 기능을 안 열어준 것이다.
> 그냥 넘어가라. **없어도 광고 제작은 전부 가능하다.** (미리보기 기능만 못 쓴다)
> 사용자에게 "이건 없어도 됩니다"라고 안심시키고 3단계로 간다.

---

## 3단계 — 토큰 받기

사용자에게:

> 이제 열쇠(토큰)를 만듭니다. 긴 영문자 문자열이 하나 나올 거예요.

1. https://developers.facebook.com/tools/explorer 접속
2. 오른쪽 위 **Meta App** 드롭다운 → 방금 만든 앱 선택
3. 그 아래 **User or Page** → **User Token** 선택
4. **Permissions**(권한) 칸에 아래를 하나씩 입력해 추가:
   ```
   ads_management       ← 필수. 광고를 만들고 수정
   ads_read             ← 성과 조회
   pages_show_list      ← 페이지 목록 조회
   pages_manage_ads     ← 페이지로 광고 게시
   ads_mcp_management   ← 2단계 6번을 했으면 추가 (없으면 건너뜀)
   ```
5. **Generate Access Token** 클릭 → 페이스북 로그인 승인 창에서 허용
6. 위에 나온 긴 문자열(`EAA...`로 시작) 전체 복사

### 토큰 저장 — 네가 대신 한다

사용자가 토큰을 채팅에 붙여넣으면 **네가 파일로 저장하라.** 사용자에게 명령어를 시키지 마라.

```bash
# macOS / Linux
printf '%s' "<받은토큰>" > .token && chmod 600 .token
```
```powershell
# Windows
[System.IO.File]::WriteAllText("$PWD\.token", "<받은토큰>")
```

저장 위치는 **이 SETUP.md와 같은 폴더**다.

저장한 뒤 사용자에게 알린다:
> 저장했습니다. 이 토큰은 광고 계정을 여는 열쇠라서, 이 대화창은 나중에 지우시는 게 좋습니다.

### 확인

```bash
cd scripts
python3 setup_wizard.py check
```

이 명령이 **토큰 권한 + Graph API + MCP 서버 세 가지를 실제로 찔러본다.** 결과 해석:

| 출력 | 뜻 | 조치 |
|---|---|---|
| `[O] 사용 가능한 토큰입니다` | 정상 | 4단계로 |
| `Graph API : X` | 치명적. 아무것도 못 함 | 권한 누락 또는 광고계정 접근 권한 없음. 3단계 다시 |
| `MCP 서버 : X` | **문제 없음** | 광고 제작은 전부 가능. 미리보기만 못 씀. 그대로 4단계로 |
| `꼭 필요한 권한이 없습니다` | `ads_management` 빠짐 | 3단계 4번을 다시 |
| `토큰을 확인할 수 없습니다` | 복사 실수 또는 만료 | 3단계 5~6번 다시 |

⚠ **MCP가 X로 나와도 놀라게 하지 마라.** "미리보기 기능만 못 쓰고 광고 만드는 건 다 됩니다"라고 알려주면 된다.

---

## 4단계 — 광고 계정·페이지 찾기 (네가 자동으로 한다)

```bash
python3 setup_wizard.py discover
```

광고계정·페이지·인스타그램·픽셀 목록이 나온다.

| 상황 | 할 일 |
|---|---|
| 후보가 **하나뿐** | 물어보지 말고 그대로 쓴다 |
| 후보가 **여러 개** | 목록을 보여주고 번호로 고르게 한다 |
| 광고계정이 **0개** | 그 계정에 광고계정 권한이 없다. 0단계 B로 돌아간다 |
| 픽셀 **없음** | 그대로 진행. "구매 전환 최적화 광고는 픽셀이 필요하다"고만 알린다 |
| 인스타 **없음** | 그대로 진행. "인스타그램 지면 노출이 제한될 수 있다"고 알린다 |

물어볼 때는 이런 식으로:

> 광고 계정이 2개 있습니다. 어느 걸 쓰시겠어요?
> 1. 12345678 — 메인 계정 (KRW)
> 2. 87654321 — 테스트 계정 (KRW)

같이 물어볼 것 (한 번에 몰아서):
- 회사/브랜드 이름
- 광고에 표시할 홈페이지 주소 (예: `example.com`)
- 하루 광고비 기본값 (기본 100,000원)

### 저장

```bash
python3 setup_wizard.py write \
  --account <광고계정ID> \
  --page <페이지ID> \
  --pixel <픽셀ID> \
  --ig <인스타ID> \
  --company "<회사이름>" \
  --display-link "<도메인>" \
  --budget 100000 \
  --cta SHOP_NOW
```

`--pixel`·`--ig`는 없으면 빼도 된다.
`--cta`는 버튼 문구: `SHOP_NOW`(지금 쇼핑하기) `ORDER_NOW`(지금 주문하기) `LEARN_MORE`(더 알아보기) `SIGN_UP`(가입하기)

### 확인

```bash
python3 setup_wizard.py verify
```

전부 `[O]`면 세팅 완료다.

---

## 5단계 — 스킬로 설치 (선택)

폴더를 스킬 위치에 두면 앞으로 **"소재 라이브해줘"** 한마디로 부를 수 있다.

```bash
# macOS / Linux
mkdir -p ~/.claude/skills && cp -R . ~/.claude/skills/meta-ad-live
```
```powershell
# Windows
New-Item -ItemType Directory -Force "$env:USERPROFILE\.claude\skills" | Out-Null
Copy-Item -Recurse -Force . "$env:USERPROFILE\.claude\skills\meta-ad-live"
```

⚠ 복사한 뒤 **Claude Code를 완전히 종료했다 다시 열어야** 인식된다.
설치하지 않고 이 폴더 그대로 써도 된다 — "이 폴더의 SKILL.md 보고 광고 만들어줘"라고 하면 된다.

---

## 세팅 후 첫 사용 안내

> 세팅 끝났습니다. 이제 소재 폴더를 주시면 광고를 만들어 드립니다.
>
> **"이 폴더 소재로 광고 만들어줘"** + 폴더 경로
>
> 소재 파일 이름을 `제품명_소구점.mp4` 형태로 지어주시면 제가 제품과 강조점을 알아서 파악합니다.
> (예: `여름원피스_시원함.mp4`)
>
> 광고는 **전부 꺼진 상태로** 만들어집니다. 확인하신 뒤 "켜줘"라고 하시면
> 세 번 확인한 다음에 켭니다. **켜지기 전까지는 돈이 나가지 않습니다.**

---

## 제품 목록 (선택 — 나중에 해도 된다)

`reference/products.json`에 제품과 판매 페이지 주소를 넣어두면,
소재 파일명만 보고 **랜딩 페이지까지 자동으로** 붙는다.

```json
[
  {"name": "제품A", "url": "https://example.com/product/a"},
  {"name": "제품B", "url": "https://example.com/product/b", "ad_url": "https://example.com/event/b"}
]
```
- `name` — 소재 파일명 앞에 쓰는 제품 이름
- `url` — 기본 판매 페이지 / `ad_url` — 광고 전용 페이지가 따로 있으면 (이쪽 우선)

비워둬도 동작한다. 그 경우 광고마다 랜딩 주소를 물어본다.

---

## 안 될 때

| 증상 | 원인·조치 |
|---|---|
| `python: command not found` | 파이썬 미설치 또는 PATH 미등록 → 1단계 |
| `config.json 이 없습니다` | 4단계 `write`를 아직 안 했다 |
| `토큰을 찾을 수 없습니다` | `.token` 파일이 없다 → 3단계 |
| `401` | 토큰 만료·오타 → 3단계 재발급 |
| `꼭 필요한 권한이 없습니다` | `ads_management` 빠짐 → 3단계 4번 |
| MCP만 X | 정상. 미리보기만 못 쓴다. 그대로 사용 |
| `광고계정을 찾을 수 없습니다` | 그 계정에 광고계정 권한 없음 → 0단계 B |
| Claude가 스킬을 못 찾음 | 5단계 후 Claude Code를 완전히 껐다 켰는지 확인 |

---

## Claude가 지켜야 할 것

- **토큰을 화면에 출력하지 마라.** 파일에 쓰고 "저장했습니다"까지만.
- 세팅 중에는 **광고를 만들지 마라.** `setup_wizard.py`·`check_token.py` 외 다른 스크립트를 실행하지 않는다.
- 사용자가 "알아서 해줘"라고 해도 **광고계정 선택만은 반드시 확인**받아라. 엉뚱한 계정에 광고비가 나가면 되돌릴 수 없다.
- 0~3단계에서 막히면 **다음 단계로 넘어가지 마라.** 앞 단계가 안 되면 뒤는 전부 실패한다.
